version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
